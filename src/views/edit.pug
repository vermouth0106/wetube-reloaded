extends base.pug

block content 
    h1 edit video!

    form(method="POST")
        input(name="title" placeholder="New Title", value=video.title, required)
        input(type="submit", value="Save")